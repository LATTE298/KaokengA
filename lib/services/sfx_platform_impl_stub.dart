/// Stub — provides the factory API used by [sfx_provider.dart].
/// The real implementations live in [sfx_io.dart] (native) and
/// [sfx_web.dart] (web).  This file must never be imported directly.
library;

import 'sfx_player.dart';

SfxPlayer makeSfxPlayer() => throw UnsupportedError('stub');
